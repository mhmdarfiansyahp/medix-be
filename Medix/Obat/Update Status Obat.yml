info:
  name: Update Status Obat
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/medicines/1/status"
  body:
    type: json
    data: |-
      {
        "is_active": true
      }
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
