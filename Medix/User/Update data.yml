info:
  name: Update data
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/users/3"
  body:
    type: json
    data: |-
      {
        "nama_user": "Budi Santoso Edit",
        "no_telp": "089876543210",
        "role": "owner",
        "status": "nonaktif"
      }
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
