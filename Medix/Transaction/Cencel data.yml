info:
  name: Cencel data
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/transactions/2/cancel"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
