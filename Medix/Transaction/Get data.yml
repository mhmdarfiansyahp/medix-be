info:
  name: Get data
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/transactions/3"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
