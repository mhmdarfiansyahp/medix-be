info:
  name: Get All
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/type-drugs"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
