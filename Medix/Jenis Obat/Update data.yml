info:
  name: Update data
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/type-drugs/1"
  body:
    type: json
    data: |-
      {
        "nama_jenis": "Kapsul Herbal"
      }
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
