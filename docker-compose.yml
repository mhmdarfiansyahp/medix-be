services:
  postgres:
    image: postgres:16-alpine
    container_name: medix-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: medix
    ports:
      - "5432:5432"
    volumes:
      - medix-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d medix"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  medix-postgres-data:
